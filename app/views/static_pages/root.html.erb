<% if current_user %>
  <script>
    window.currentUser = {
      "id": <%= current_user.id %>,
      "email": '<%= current_user.email %>',
      "username": '<%= current_user.username %>',
      "name": '<%= current_user.name %>',
    };
  </script>
<% end%>


<main id="root">Hello from Root</main>